# Templates

`sample-template.docx` is the MVP acceptance fixture. It contains tagged Word Content Controls:

- `TEN_KHACH_HANG`
- `SO_HO_SO`

Production templates should remain approved `.docx` files and should use tagged Content Controls for fields that the add-in fills.
